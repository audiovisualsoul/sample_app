<div  style="text-align:center;">
<div class="contentPad">
<h2>Sign up</h2>
<%= form_for(@user) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
  </div>
<% end %>
